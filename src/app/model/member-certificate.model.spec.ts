import { MemberCertificate } from './member-certificate.model';

describe('MemberCertificate', () => {
  it('should create an instance', () => {
    expect(new MemberCertificate()).toBeTruthy();
  });
});
